Tabs i två stilar: segmenterad kontroll (standard, för vyväxling) och understruken rad (`line`, för rapportsektioner).

```jsx
<Tabs tabs={[{id:"d",label:"Dag"},{id:"v",label:"Vecka"}]} activeId="d" onSelect={setPeriod} />
```

